<%= render 'shared/errors', resource: post %>
<%= form_with model: post, local: true do |f|  %>
  <%= f.label :name%>
  <%= f.text_area :name %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit 'POST' %>
<% end %>